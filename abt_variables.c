// we have char int short long and long long for integers
// and we have floats for well floats decimals also double ig mm
// we dont have boolean so we have to define it mm 
// defining variables
#include <stdio.h>

int main() {
    int this_is_an_interger;
    int a = 10;
    int b = 20, c = 20;
    this_is_an_interger = b + c - a;
    printf("%d", this_is_an_interger);}
